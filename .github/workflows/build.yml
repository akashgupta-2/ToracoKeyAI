name: Build ToracoKey AI v0.19

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check v0.19 ZIP
        run: |
          test -f ToracoKeyAI_v0.19_SafeModeSwitch_FINAL.zip
          echo "Found v0.19 ZIP"

      - name: Extract v0.19 ZIP
        run: |
          rm -rf extracted
          mkdir extracted
          unzip -q ToracoKeyAI_v0.19_SafeModeSwitch_FINAL.zip -d extracted
          test -f extracted/v017/settings.gradle.kts

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Build Debug APK
        working-directory: extracted/v017
        run: gradle :app:assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ToracoKeyAI-v0.19-debug
          path: extracted/v017/app/build/outputs/apk/debug/app-debug.apk
